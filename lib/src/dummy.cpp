#include <mg_gpgpu/core/gpu/dummy.h>

//dummy code just to not have an empty library, not sure if I want to keep the static lib or ensure
// header only
void test()
{
	std::cout<<"dummy test"<<std::endl;
}
	
